<?xml version="1.0"?>
<openerp>
    <data>

        <record id="view_calendar_event_form" model="ir.ui.view">
            <field name="model">calendar.event</field>
            <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
            <field name="arch" type="xml">
                <field name="duration" position="after">
                    <field name="week_number"/>
<!--
                    <field name="day"/>
-->
                </field>
            </field>
        </record>

        <!-- Calendar Kanban -->
        <record id="view_calendar_event_kanban" model="ir.ui.view">
            <field name="name">Meetings Calendar</field>
            <field name="model">calendar.event</field>
            <field name="arch" type="xml">
                <kanban class="oe_background_grey" default_group_by="">
                    <field name="color"/>
                    <field name="name" string="Subject"/>
                    <field name="allday" invisible="True"/>
                    <field name="start_date" string="Date"/>
                    <field name="start_datetime" string="Date"/>
                    <field name="user_id"/>
                    <field name="location"/>
                    <field name="show_as"/>
                    <field name="class" string="Privacy"/>
                    <field name="state" invisible="True"/>
                    <field name="duration"/>
                    <field name="week_number"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="oe_kanban_color_#{kanban_getcolor(record.color.raw_value)} oe_kanban_card oe_kanban_project oe_kanban_global_click">
                                <div class="oe_dropdown_toggle oe_dropdown_kanban" groups="base.group_user">
                                    <span class="oe_e">í</span>
                                    <ul class="oe_dropdown_menu">
                                        <t t-if="widget.view.is_action_enabled('edit')"><li><a type="edit">Event Settings</a></li></t>
                                        <t t-if="widget.view.is_action_enabled('delete')"><li><a type="delete">Delete</a></li></t>
                                        <li><ul class="oe_kanban_colorpicker" data-field="color"/></li>
                                    </ul>
                                </div>
                                <div class="oe_kanban_content">
                                    <h4 class="text-center"><strong><field name="name"/></strong></h4>
                                </div>
                                <div class="text-center oe_kanban_alias">
                                    <small><i class="fa fa-envelope-o"></i> <field name="location"/></small>
                                </div>
                                <div class="oe_clear"></div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <record id="calendar.action_calendar_event" model="ir.actions.act_window">
            <field name="view_mode">kanban,calendar,tree,form,gantt</field>
        </record>

        <record id="action_view_calendar_event_kanban" model="ir.actions.act_window.view">
            <field name="act_window_id" ref="calendar.action_calendar_event"/>
            <field name="sequence" eval="1"/>
            <field name="view_mode">kanban</field>
            <field name="view_id" ref="view_calendar_event_kanban"/>
        </record>

        <record model="ir.actions.server" id="action_create_booking">
            <field name="name">Create Meetings</field>
            <field name="state">code</field>
            <field name="condition" eval="True"/>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="code">for o in object.browse(context.get('active_ids', [])):
    env['calendar.event'].create({
    'name': o.name,
    'start_datetime': '2000-01-01 00:00:00',
    'stop_datetime': '2000-01-01 00:00:00',
    })
            </field>
        </record>

        <record model="ir.values" id="action_create_booking_menu">
            <field name="name">Create Meetings</field>
            <field name="model">res.partner</field>
            <field name="key2">client_action_multi</field>
            <field name="value" eval="'ir.actions.server,%d' % action_create_booking"/>
        </record>

<!--
        <record model="ir.actions.server" id="action_create_phonecall">
            <field name="name">Create Phone Call</field>
            <field name="state">code</field>
            <field name="condition" eval="True"/>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="code">for o in object.browse(context.get('active_ids', [])):
env['crm.phonecall'].create({
'name': o.name,
'partner_id': o.id,
})
            </field>
        </record>

        <record model="ir.values" id="action_create_phonecall_menu">
            <field name="name">Create Phone Call</field>
            <field name="model">res.partner</field>
            <field name="key2">client_action_multi</field>
            <field name="value" eval="'ir.actions.server,%d' % action_create_phonecall"/>
        </record>
-->

    </data>
</openerp>
